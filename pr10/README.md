Практична робота №10 з дисципліни АСПз Виконано Дрогобицьким Володимиром, в середовищі UTM Linux ubuntu server 
Тема практичної роботи: Signals

***Загальна інформація***

У даній практичній роботі для виконання загального завдання необхідно протестувати 3 приклади коду з лекції №13, 
Перша програма представлена у файлі crash_diag.c.

***Виконання загального завдання №1:***

Для того аби програма повністю функціонувала, на моїй системі мені потрібно було адаптувати код, адже я використовую систему aarch64,

Що було змінено в коді(добавлено):
```c
#if defined(aarch64)
    if (ctx != NULL) {
        ucontext_t *uc = (ucontext_t *)ctx;
        struct sigcontext *sc = &uc->uc_mcontext;

        wr("PC:  "); wr_hex(sc->pc);  wr("\n");
        wr("SP:  "); wr_hex(sc->sp);  wr("\n");
        wr("PSTATE: "); wr_hex(sc->pstate); wr("\n");
        
        // Виведемо перші кілька загальних регістрів
        wr("X0:  "); wr_hex(sc->regs[0]); wr("\n");
        wr("X1:  "); wr_hex(sc->regs[1]); wr("\n");
        wr("X2:  "); wr_hex(sc->regs[2]); wr("\n");
        wr("X29 (FP): "); wr_hex(sc->regs[29]); wr("\n");
        wr("X30 (LR): "); wr_hex(sc->regs[30]); wr("\n");
    }

```

Компіляція та запуск даного проекту відбувалось за допомогою такої команди:
```bash
gcc -Wall -Wextra -O0 -g -fno-omit-frame-pointer -no-pie crash_diag.c -o crash_diag
./crash_diag
```

Вивід в консоль:
```bash
gcc -Wall -Wextra -O0 -g -fno-omit-frame-pointer -no-pie crash_diag.c -o crash_diag
./crash_diag                                 ^
About to crash. PID=2373

=== crash captured ===
signal: 11
si_code: 1
fault address: 0x0
PC:  0x400ef4
SP:  0xffffeb870ad0
PSTATE: 0x60000000
X0:  0x0
X1:  0x2a
X2:  0x1
X29 (FP): 0xffffeb870ae0
X30 (LR): 0x400f38
```

Також було виконанно пошук рядка за RIP за допомогою команди(рядок також модифікований відповідно до типу системи):
```bash
addr2line -e ./crash_diag -f -C 0x400ef4
```
Вивід в консоль:
```bash
addr2line -e ./crash_diag -f -C 0x400ef4
crash_here
/home/drogobytskyyy/System-software-architecture-study-repo/pr10/crash_diag.c:147
```

Висновок до тестування цього проекту:

  Адаптація коду під архітектуру: Оригінальний код було успішно модифіковано для роботи з процесорами ARM64. Це було досягнуто шляхом звернення до структури sigcontext всередині контексту сигналу (ucontext_t), що дозволило отримати доступ до специфічних регістрів: PC (Program Counter) та X0-X30.

  Діагностика сигналу: Програма коректно перехопила сигнал SIGSEGV (номер 11), що виник внаслідок спроби запису за нульовою адресою (fault address: 0x0). Код помилки si_code: 1 підтвердив, що це була помилка відображення пам'яті (SEGV_MAPERR).

Аналіз стану процесора:

Регістр X0 зі значенням 0x0 чітко вказав на джерело помилки (нульовий вказівник).
Регістр X1 містив значення 0x2a (42 у десятковій системі), що відповідає константі, яку програма намагалася записати в пам'ять.

  Верифікація місця аварії: Використання утиліти addr2line з отриманою адресою PC (0x400ef4) дозволило точно ідентифікувати локалізацію помилки: функція crash_here, рядок 147. Це доводить ефективність використання налагоджувальних символів (прапорець -g) для post-mortem аналізу.

Підсумок:
Програма crash_diag.c продемонструвала надійний метод самодіагностики ПЗ. Такий підхід дозволяє розробнику отримувати критично важливу інформацію про стан регістрів та стека безпосередньо в момент краху, що є незамінним при відлагодженні системного ПЗ у середовищах, де використання інтерактивних дебагерів (як GDB) є обмеженим або неможливим.

***Виконання загального завдання №2:***

Друга програма представлена у файлі sleep_corect.c.

Компіляція та запуск даного проекту відбувалось за допомогою такої команди:
```bash
gcc -Wall -Wextra -O2 sleep_correct.c -o sleep_correct
./sleep_correct
```

Також для демонстрування роботи даної програми, під час її виконання було виконано команду:
```bash
kill -USR1 <pid>
```
У моєму випадку pid = 4451

Вивід консоль 1:
```bash
./sleep_correct
PID=4451. In another terminal: kill -USR1 4451
Relative sleep for 5 seconds using nanosleep restart loop...
Relative sleep finished. got_usr1=1
Now 5 periodic ticks with absolute clock_nanosleep deadlines...
tick 1
tick 2
tick 3
tick 4
tick 5
```

Ввід в консоль 2:
```bash
kill -USR1 4451
```

Висновок до тестування цього проекту:
 
   Обробка переривань (EINTR): Тестування підтвердило, що використання nanosleep у циклі з перевіркою errno == EINTR дозволяє програмі ігнорувати втручання зовнішніх сигналів. При надсиланні сигналу SIGUSR1 програма не завершила виконання передчасно, а використала структуру rem (remaining time) для повторного запуску таймера на залишок часу.

   Атомарність сигналів: Змінна got_usr1 типу volatile sig_atomic_t коректно змінила свій стан усередині обробника сигналу, що було підтверджено виводом у консоль після завершення першої фази сну.

   Використання абсолютних дедлайнів: Друга частина програми продемонструвала роботу clock_nanosleep із прапорцем TIMER_ABSTIME. Цей підхід є найбільш коректним для реалізації періодичних завдань (наприклад, зняття показників датчиків або мережевих тіків), оскільки він прив'язаний до монотонного годинника системи (CLOCK_MONOTONIC) і запобігає накопиченню часового дрейфу (drift), викликаного затримками виконання самого коду між викликами таймера.

Підсумок:

   Програма sleep_correct.c демонструє стандарти написання відмовостійкого системного коду. Головний висновок полягає в тому, що для надійних систем реального часу слід уникати простих функцій sleep(), віддаючи перевагу clock_nanosleep з абсолютними значеннями часу, що гарантує точність інтервалів навіть за умов активного використання сигналів у системі.


***Виконання загального завдання №3:***

Третя програма представлена у файлі: rt_pubsub.c

Під час виконання тестування даного проекту, було використано два різні термінали, для коретного виклику та роботи з дочірнім процесом.

Компіляція даного проекту відбувалась за допомогою команди:
```bash
gcc -Wall -Wextra -O2 rt_pubsub.c -o rt_pubsub
```

Для запуску з першого терміналу(основного) для початку виконується команда:
```bash
./rt_pubsub sub
```

Тобто таким чином ми запускаємо підписника(sub).

У другому терміналі ви виконуємо таку команду:
```bash
./rt_pubsub pub <PID>  5 25 125 -1
```

У нашому випадку PID = 2088, тому відповідно команда:
```bash
./rt_pubsub pub 2088  5 25 125 -1
```

Після цього можемо спостерігати вивід у двох консолях:
Вивід консолі №1:
```bash
./rt_pubsub sub
subscriber PID=2088, waiting for signal 34 (SIGRTMIN)
received signal=34 value=5 from pid=2181 uid=1000
received signal=34 value=25 from pid=2181 uid=1000
received signal=34 value=125 from pid=2181 uid=1000
received signal=34 value=-1 from pid=2181 uid=1000
negative value received: shutting down subscriber
```

Вивід консолі №2:
```bash
./rt_pubsub pub 2088  5 25 125 -1
sent value=5 to pid=2088 via signal=34
sent value=25 to pid=2088 via signal=34
sent value=125 to pid=2088 via signal=34
sent value=-1 to pid=2088 via signal=34
```

Після виконання запуску та компіляції ми можемо спостерігати як програма правильно виконує поставленну задачу та працює до отримання негативного значення.

Коли ми тестуємо даний проект у другому режимі тобто використовуючи відповідну команду:
```bash
./rt_pubsub sub-timeout
```

Ми запускаємо програму у трішки іншому форматі, вона очікує відповіді з іншого терміналу, та кожні 5 секунд якщо не отримала нічого, сповіщає про це.

Вивід в консолі №1:
```bash
./rt_pubsub sub-timeout
subscriber PID=2236, waiting for signal 34 (SIGRTMIN)
timeout: no messages for 5 seconds
timeout: no messages for 5 seconds
timeout: no messages for 5 seconds
received signal=34 value=5 from pid=2243 uid=1000
received signal=34 value=25 from pid=2243 uid=1000
received signal=34 value=125 from pid=2243 uid=1000
received signal=34 value=-1 from pid=2243 uid=1000
negative value received: shutting down subscriber
```

Вивід в консолі №2:
```bash
./rt_pubsub pub 2236  5 25 125 -1
sent value=5 to pid=2236 via signal=34
sent value=25 to pid=2236 via signal=34
sent value=125 to pid=2236 via signal=34
sent value=-1 to pid=2236 via signal=34
```

Як ми можемо спостерігати програма виводить timeout: no messages for 5 seconds, що означає що проект працює як очікувано.

Висновок:

У ході виконання третього завдання було досліджено механізм міжпроцесної взаємодії за допомогою сигналів реального часу (POSIX real-time signals). 
На відміну від стандартних сигналів, використання sigqueue та sigwaitinfo дозволило реалізувати повноцінну модель «Видавець-Підписник» 
(Pub/Sub) з можливістю передачі цілочисельних даних разом із сигналом. 
Тестування підтвердило, що сигнали реального часу надійно стають у чергу, не втрачаючись при інтенсивному надсиланні,
а використання функції sigtimedwait забезпечує коректну обробку тайм-аутів у разі відсутності повідомлень.

Головна перевага продемонстрованого підходу полягає в асинхронності та можливості передачі контексту (payload) 
без використання складних механізмів спільної пам'яті. Блокування сигналів через sigprocmask перед їх очікуванням гарантує, 
що повідомлення не будуть оброблені стандартними системними хендлерами, 
а будуть отримані саме в той момент, коли підписник готовий до їх обробки. 
Це робить дану архітектуру ефективною для побудови легких систем обміну подіями в реальному часі.


Завдання за варіантом №6(Скриншот завдання):
<img width="599" height="63" alt="Знімок екрана 2026-05-06 о 16 51 37" src="https://github.com/user-attachments/assets/50581669-fa1f-45cc-bdea-d6964b9daccf" />

Відповідь:

Виконання даного завдання реалізовано у файлі task_by_var_6.c, 
що ми робимо: в циклі створюємо 5 дочірніх процесів, та правильно їх завершуєм, 
головна суть не допустити рекурсивного створення дочірніх процесів дочірніми процесами, для цього реалізовано такий цикл:
```c
int n = 5;

...

for (int i = 0; i < n; i++) {
    wait(NULL);
}
```

Все в сумі дозволяє нам повноцінно виконати дане завдання та отримати очікуваний результат.

Вивід в консоль:
```bash
./task

Fork process №1 (PID: 3236)
Fork process №2 (PID: 3237)
Fork process №4 (PID: 3239)
Fork process №3 (PID: 3238)
Fork process №5 (PID: 3240)
All PIDs finished, program finished.
```

Висновок:

Виконання цього завдання демонструє фундаментальні принципи багатопроцесного програмування в UNIX-подібних системах за допомогою системного виклику fork(). 
Ключовим аспектом є чітке розмежування логіки: дочірні процеси виконують свою частину роботи та обов'язково завершуються через exit(), 
щоб запобігти неконтрольованому розмноженню ("рекурсивному" розгалуженню в циклі). 
Це забезпечує стабільність системи та гарантує, що кожен з п'яти процесів виконає лише своє індивідуальне завдання.

З боку батьківського процесу критично важливою є синхронізація за допомогою виклику wait(). 
Це дозволяє основній програмі контролювати життєвий цикл своїх "нащадків", збирати ресурси після їх завершення та уникати появи процесів-зомбі.
Отриманий результат ілюструє асинхронну природу процесів: хоча вони створюються послідовно, 
черговість їх виводу в консоль залежить від планувальника операційної системи, що є типовим для паралельного виконання задач.


